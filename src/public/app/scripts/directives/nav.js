'use strict';

angular.module('site-main').directive('nav', function () {

  return {

    restrict: 'E',
    link: function (scope, element, attrs) {

      //element.text('this is the nav directive');
    }
  };
});
