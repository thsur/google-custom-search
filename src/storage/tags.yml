# tags.yml
#
# This file gets overwritten by certain tasks, though you
# should be safe to add and remove tags manually (but not,
# for example, uncomment them - those would be gone).
#
main:
    - one
    - two
    - three
subsidiary:
    - simplex
    - com_plex
    - xelp_moc_xelpmis
